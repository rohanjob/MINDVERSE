import React from 'react';

const CosmicParticles = () => {
    return (
        <div className="cosmic-particles-overlay">
            {/* This component provides the cosmic particles effect */}
            {/* Actual particles are created in App.js via DOM manipulation for performance */}
        </div>
    );
};

export default CosmicParticles;
